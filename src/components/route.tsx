import { CatmullRomCurve3 } from 'three';

import { Line } from '@react-three/drei';

export default function RouteLine({ curve }: { curve: CatmullRomCurve3 }) {
  // return <Line points={curve.getPoints(100)} color='red' lineWidth={2} />;
}
